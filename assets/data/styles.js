(() => {
  "use strict";

  // Compatibility style used until numbered theme file trios are implemented.
  // Add a style here only after its matching HTML, JavaScript, and CSS files exist.
  window.PORTFOLIO_STYLE_POOL = Object.freeze([
    Object.freeze({
      id: "base-monochrome",
      classNames: Object.freeze(["theme-monochrome", "layout-single"]),
      variables: Object.freeze({})
    })
  ]);
})();
